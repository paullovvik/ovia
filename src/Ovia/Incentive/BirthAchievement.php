<?php

namespace Ovia\Incentive;

/**
 * BirthAchievement represents a birth that was recorded by a user.
 */
class BirthAchievement extends AbstractAchievement
{
    // @todo - Need to record the child's name or other identifying information
    // so this achievement will not be awarded more than once per child, but
    // will allow an achievement for each child.
}
